[project]
name = "email-rl-env"
version = "0.1.0"
description = "Real-world RL environment for email inbox management"
authors = [{ name = "Shaik Asif" }]
requires-python = ">=3.9"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai"
]